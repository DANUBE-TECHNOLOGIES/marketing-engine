"use strict";

const STOPWORDS = new Set(
  "a au aux avec ce ces dans de des du elle en et eux il je la le les leur lui ma mais me meme mes moi mon ne nos notre nous on ou par pas pour qu que qui sa se ses son sur ta te tes toi ton tu un une vos votre vous c est sont plus agence agences voyage voyages mondescale fram".split(" ")
);
const PRESENTATION_KEYS = new Set("align alignment background backgroundcolor backgroundimage borderradius class classname color columns ctavariant display height href icon id image imageurl key layout mode objectfit position primary secondary size slug style target theme tone type url variant width".split(" "));
const PRESENTATION_VALUES = new Set("left right center centre top bottom primary secondary outline ghost light dark desktop mobile tablet contain cover auto none true false".split(" "));
const IDENTITY_KEYS = new Set("address adresse street streetaddress postalcode zipcode zip phone telephone tel email mail openinghours hours latitude longitude".split(" "));
const IDENTITY_BLOCK_TYPES = new Set(["contact","map","location","opening-hours","agency-contact"]);
function clean(value){return String(value||"").replace(/<[^>]*>/g," ").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").replace(/\s+/g," ").trim()}
function normalizedKey(key){return clean(key).replace(/\s+/g,"")}
function looksLikePresentationValue(value){const n=clean(value);if(!n)return true;if(PRESENTATION_VALUES.has(n))return true;if(/^(https?:\/\/|mailto:|tel:)/i.test(String(value||"").trim()))return true;if(/^#[0-9a-f]{3,8}$/i.test(String(value||"").trim()))return true;return false}
function collectEditorialText(value,chunks=[],key=""){if(value==null)return chunks;const k=normalizedKey(key);if(k&&(PRESENTATION_KEYS.has(k)||IDENTITY_KEYS.has(k)))return chunks;if(typeof value==="string"){if(!looksLikePresentationValue(value))chunks.push(value);return chunks}if(typeof value==="number"||typeof value==="boolean")return chunks;if(Array.isArray(value)){for(const item of value)collectEditorialText(item,chunks,key);return chunks}if(typeof value==="object")for(const [childKey,childValue] of Object.entries(value))collectEditorialText(childValue,chunks,childKey);return chunks}
function collectObjectText(value,chunks=[]){return collectEditorialText(value,chunks)}
function blockText(block){if(IDENTITY_BLOCK_TYPES.has(clean(block?.blockType)))return "";const chunks=[];collectEditorialText(block?.content||{},chunks);return clean(chunks.filter(Boolean).join(" "))}
function sectionText(section){const chunks=[];collectEditorialText(section?.jsonContent||{},chunks);return clean(chunks.filter(Boolean).join(" "))}
function pageFragments(page){const fragments=[];const metaText=clean([page?.title,page?.h1,page?.seoTitle,page?.metaDescription].filter(Boolean).join(" "));if(metaText)fragments.push({source:"meta",id:null,type:"page-meta",name:"Métadonnées de page",displayOrder:-1,text:metaText});for(const [index,section] of(page?.sections||[]).entries()){const text=sectionText(section);if(text)fragments.push({source:"section",id:section?.id||null,type:section?.sectionType||"section",name:section?.sectionType||`Section ${index+1}`,displayOrder:Number.isFinite(section?.displayOrder)?section.displayOrder:index,text})}for(const [index,block] of(page?.blocks||[]).entries()){const text=blockText(block);if(text)fragments.push({source:"block",id:block?.id||null,type:block?.blockType||"block",name:block?.name||block?.blockType||`Bloc ${index+1}`,displayOrder:Number.isFinite(block?.displayOrder)?block.displayOrder:index,text})}return fragments}
function pageText(page){return clean(pageFragments(page).map(f=>f.text).join(" "))}
function shingles(text,size=5){const words=clean(text).split(" ").filter(Boolean),out=new Set();for(let i=0;i<=words.length-size;i++)out.add(words.slice(i,i+size).join(" "));return out}
function similarity(a,b){const A=shingles(a),B=shingles(b);if(!A.size||!B.size)return 0;let intersection=0;for(const item of A)if(B.has(item))intersection++;return intersection/Math.min(A.size,B.size)}
function sharedSegments(a,b,size=8,limit=8){const words=clean(a).split(" ").filter(Boolean),other=shingles(b,size),segments=[];for(let i=0;i<=words.length-size;i++){const phrase=words.slice(i,i+size).join(" ");if(other.has(phrase)&&!segments.some(s=>s.includes(phrase)||phrase.includes(s)))segments.push(phrase);if(segments.length>=limit)break}return segments}
function agencyIdOf(page){return page?.site?.agencyId??page?.site?.agency?.id??null}
function isSameAgency(target,candidate){const a=agencyIdOf(target),b=agencyIdOf(candidate);return a!=null&&b!=null&&String(a)===String(b)}
function distinctiveTerms(text,limit=12){const counts=new Map();for(const word of clean(text).split(" ")){if(word.length<4||STOPWORDS.has(word)||/^\d+$/.test(word))continue;counts.set(word,(counts.get(word)||0)+1)}return[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])).slice(0,limit).map(([term,count])=>({term,count}))}
function recommendations(target,matches,segments){const agency=target?.site?.agency||{},city=String(agency.city||"").trim(),name=String(agency.name||target?.site?.name||"").trim(),ideas=[];if(segments.length)ideas.push({priority:"high",code:"REWRITE_SHARED_SEGMENTS",title:"Réécrire les passages communs au réseau",detail:`${segments.length} segment(s) de formulation sont partagés avec les pages d'autres agences. Conserver le fond métier, mais reformuler avec des preuves propres à ${name||"cette agence"}.`});ideas.push({priority:"high",code:"ADD_LOCAL_PROOFS",title:"Ajouter des preuves locales vérifiables",detail:`Renforcer la page avec des éléments réellement propres à l'agence${city?` de ${city}`:""} : équipe, accès, zones de clientèle, spécialités attestées, avis locaux, partenariats et habitudes de départ.`});ideas.push({priority:"medium",code:"LOCAL_INTENT_COPY",title:"Renforcer l'intention de recherche locale",detail:`Développer des formulations naturelles liées à ${city||"la ville"} et aux besoins des voyageurs locaux, sans répéter artificiellement le nom de la commune.`});if(matches.length)ideas.push({priority:"medium",code:"DIFFERENTIATE_FROM_NEAREST",title:"Se différencier des agences les plus proches éditorialement",detail:`Priorité de différenciation face à ${matches.slice(0,3).map(m=>m.agencyName).join(", ")}.`});return ideas}
function blockRecommendation(fragment,nearest,segments,target){const city=String(target?.site?.agency?.city||"").trim();if(!segments.length)return null;return{priority:nearest>=0.7?"high":"medium",code:"DIFFERENTIATE_BLOCK_COPY",title:`Différencier le bloc « ${fragment.name} »`,detail:`Ce bloc partage ${segments.length} formulation(s) avec d'autres agences du réseau. Remplacer les formulations génériques par des faits vérifiables${city?` propres à ${city}`:" propres à l'agence"}.`}}
function networkCandidates(target,candidates){return candidates.filter(page=>page.id!==target.id&&!isSameAgency(target,page))}
function internalCandidates(target,candidates){return candidates.filter(page=>page.id!==target.id&&isSameAgency(target,page))}
function analyzeBlockInsights(target,candidates){const targetBlocks=pageFragments(target).filter(f=>f.source==="block"&&f.text.split(" ").filter(Boolean).length>=12),candidateFragments=networkCandidates(target,candidates).flatMap(page=>pageFragments(page).filter(f=>f.source==="block").map(fragment=>({page,fragment})));return targetBlocks.map(fragment=>{const nearestMatches=candidateFragments.map(({page,fragment:other})=>({pageId:page.id,agencyId:agencyIdOf(page),agencyName:page?.site?.agency?.name||page?.site?.name||"Autre agence",slug:page.slug,blockId:other.id,blockType:other.type,similarity:Number(similarity(fragment.text,other.text).toFixed(3)),sharedSegments:sharedSegments(fragment.text,other.text,8,4)})).filter(m=>m.similarity>=0.2||m.sharedSegments.length).sort((a,b)=>b.similarity-a.similarity).slice(0,3),highestSimilarity=nearestMatches[0]?.similarity||0,segments=nearestMatches.flatMap(m=>m.sharedSegments).filter((v,i,a)=>a.indexOf(v)===i).slice(0,6),advice=blockRecommendation(fragment,highestSimilarity,segments,target);return{blockId:fragment.id,blockType:fragment.type,blockName:fragment.name,displayOrder:fragment.displayOrder,wordCount:fragment.text.split(" ").filter(Boolean).length,score:Math.max(0,Math.round((1-highestSimilarity)*100)),highestSimilarity,sharedSegments:segments,nearestMatches,recommendations:advice?[advice]:[]}}).filter(i=>i.highestSimilarity>=0.2||i.sharedSegments.length).sort((a,b)=>b.highestSimilarity-a.highestSimilarity)}
function analyzeInternalRepetition(target,candidates){const text=pageText(target);return internalCandidates(target,candidates).map(page=>({pageId:page.id,slug:page.slug,similarity:Number(similarity(text,pageText(page)).toFixed(3)),sharedSegments:sharedSegments(text,pageText(page),8,4)})).filter(x=>x.similarity>=0.2||x.sharedSegments.length).sort((a,b)=>b.similarity-a.similarity).slice(0,5)}
function analyzeUniqueness(target,candidates=[]){const text=pageText(target),network=networkCandidates(target,candidates),rawMatches=network.map(page=>({page,similarity:Number(similarity(text,pageText(page)).toFixed(3))})).filter(m=>m.similarity>=0.2).sort((a,b)=>b.similarity-a.similarity).slice(0,5),matches=rawMatches.map(({page,similarity:s})=>({pageId:page.id,siteId:page.siteId,agencyId:agencyIdOf(page),agencyName:page.site?.agency?.name||page.site?.name||"Autre agence",slug:page.slug,similarity:s})),highest=matches[0]?.similarity||0,segments=rawMatches.flatMap(m=>sharedSegments(text,pageText(m.page),8,4)).filter((v,i,a)=>a.indexOf(v)===i).slice(0,10),blockInsights=analyzeBlockInsights(target,network),internalRepetition=analyzeInternalRepetition(target,candidates);return{version:"1.4",score:Math.max(0,Math.round((1-highest)*100)),ready:highest<0.55,severity:highest>=0.7?"blocker":highest>=0.55?"warning":"ok",highestSimilarity:highest,matches,sharedSegments:segments,distinctiveTerms:distinctiveTerms(text),recommendations:recommendations(target,matches,segments),blockInsights,internalRepetition,metrics:{targetWordCount:text?text.split(" ").filter(Boolean).length:0,candidatesObserved:candidates.length,networkCandidatesObserved:network.length,internalCandidatesObserved:internalCandidates(target,candidates).length,sharedSegmentCount:segments.length,blocksObserved:pageFragments(target).filter(f=>f.source==="block").length,blocksFlagged:blockInsights.length,internalRepetitionsObserved:internalRepetition.length},note:highest>=0.7?"Contenu trop proche d'une autre agence du réseau : différenciation locale indispensable.":highest>=0.55?"Similarité inter-agences élevée : renforcer les preuves et formulations propres à l'agence.":highest>=0.4?"Page publiable, mais encore fortement issue du socle éditorial réseau : différenciation locale recommandée.":"Aucune similarité inter-agences préoccupante détectée."}}
module.exports={clean,collectEditorialText,collectObjectText,blockText,sectionText,pageFragments,pageText,shingles,similarity,sharedSegments,agencyIdOf,isSameAgency,distinctiveTerms,analyzeBlockInsights,analyzeInternalRepetition,analyzeUniqueness};
