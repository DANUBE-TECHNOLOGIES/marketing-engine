import MainLayout from "../../components/MainLayout";
export const dynamic = "force-dynamic";
function origin(){return String(process.env.BACKEND_INTERNAL_URL||process.env.API_INTERNAL_URL||"http://backend:4000").replace(/\/+$/g,"")}
async function load(){const r=await fetch(`${origin()}/api/presence/providers`,{headers:{Accept:"application/json","x-tenant-slug":process.env.NEXT_PUBLIC_TENANT_SLUG||"mondescale"},cache:"no-store"});if(!r.ok)throw new Error(`Providers Presence HTTP ${r.status}: ${await r.text()}`);return r.json()}
export default async function Page(){let data,error;try{data=await load()}catch(e){error=e}const providers=data?.providers||[];return <MainLayout title="Providers Presence" subtitle="Capacités et état opérationnel des plateformes raccordées au référentiel NAP.">{error?<div className="rounded-2xl bg-red-50 p-5 text-red-900">{error.message}</div>:<div className="grid gap-4">{providers.map(p=><div key={p.key} className="rounded-2xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-xl font-black">{p.label||p.key}</h2><div className="mt-1 text-sm text-slate-500">{p.key}</div></div><span className={`rounded-full px-3 py-1 text-xs font-black uppercase ${p.readiness?.ready?"bg-emerald-100 text-emerald-900":"bg-amber-100 text-amber-900"}`}>{p.readiness?.ready?"Prêt":"Non prêt"}</span></div><div className="mt-4 grid gap-3 md:grid-cols-3"><div><div className="text-xs uppercase text-slate-500">Mode</div><div className="font-bold">{p.mode||p.integrationMode||"—"}</div></div><div><div className="text-xs uppercase text-slate-500">Annuaire historique</div><div className="font-bold">{p.legacyDirectory?.name||"—"}</div></div><div><div className="text-xs uppercase text-slate-500">État</div><div className="font-bold">{p.readiness?.reason||p.readiness?.status||"—"}</div></div></div></div>)}{!providers.length?<div className="rounded-2xl border bg-white p-10 text-center text-slate-500">Aucun provider Presence déclaré.</div>:null}</div>}</MainLayout>}
