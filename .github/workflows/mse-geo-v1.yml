name: MSE-GEO V1 Agency Entity Graph

on:
  pull_request:
    paths:
      - "backend/src/modules/minisite-structured-data/**"
      - "backend/src/modules/public-site-read/**"
      - "backend/test/minisite-structured-data-geo-v1.test.js"
      - "backend/package.json"
      - "backend/package-lock.json"
      - "frontend/app/agence/**"
      - "frontend/lib/seo/**"
      - "frontend/test/mse-geo-v1-agency-entity-graph.test.mjs"
      - "frontend/package.json"
      - "frontend/package-lock.json"
      - ".github/workflows/mse-geo-v1.yml"
  workflow_dispatch:

jobs:
  geo-v1-backend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci
      - run: node --test test/minisite-structured-data-geo-v1.test.js

  geo-v1-canonical-frontend-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: node --test test/mse-geo-v1-agency-entity-graph.test.mjs
