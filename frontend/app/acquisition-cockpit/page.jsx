"use client";

import { useEffect, useMemo, useState } from "react";
import MainLayout from "../components/MainLayout";

const API=process.env.NEXT_PUBLIC_API_URL||"";
const AGENCIES={
"ambassade-fram-mondescale-bois-colombes":"Bois-Colombes","ambassade-fram-mondescale-ozoir-la-ferriere":"Ozoir-la-Ferrière","ambassade-fram-mondescale-maurepas":"Maurepas","ambassade-fram-mondescale-gien":"Gien","ambassade-fram-mondescale-nevers":"Nevers","ambassade-fram-mondescale-dax":"Dax","mondescale-lamorlaye":"Lamorlaye","tui-store-amilly":"Amilly","tui-store-melun":"Melun"};
const CHANNELS={iga:"IGA / Email",facebook:"Facebook",instagram:"Instagram",google:"Google Business",agency:"QR agence",direct:"Direct"};
const n=v=>Number(v||0);
const pct=(a,b)=>b?`${((100*a)/b).toFixed(1)} %`:"—";
function aggregate(rows){return rows.reduce((a,r)=>{for(const k of ["leads","hot","warm","cold","contacted","converted","notificationSent"])a[k]+=n(r[k]);a.scoreWeighted+=n(r.avgScore)*n(r.leads);return a},{leads:0,hot:0,warm:0,cold:0,contacted:0,converted:0,notificationSent:0,scoreWeighted:0})}
function Card({label,value,sub}){return <div className="bg-white rounded-2xl shadow-sm border border-slate-100 p-5"><div className="text-sm text-slate-500">{label}</div><div className="text-3xl font-bold text-[#073653] mt-1">{value}</div>{sub&&<div className="text-xs text-slate-400 mt-2">{sub}</div>}</div>}
export default function AcquisitionCockpit(){const[days,setDays]=useState(30);const[site,setSite]=useState("");const[rows,setRows]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");useEffect(()=>{let alive=true;setLoading(true);setError("");const qs=new URLSearchParams({days:String(days),by:"channel"});if(site)qs.set("siteSlug",site);fetch(`${API}/api/leads/acquisition/analytics?${qs}`).then(r=>r.json().then(b=>({r,b}))).then(({r,b})=>{if(!r.ok||!b.ok)throw new Error(b.error||`HTTP ${r.status}`);if(alive)setRows(b.funnels||[])}).catch(e=>alive&&setError(e.message)).finally(()=>alive&&setLoading(false));return()=>{alive=false}},[days,site]);const totals=useMemo(()=>aggregate(rows),[rows]);const avg=totals.leads?(totals.scoreWeighted/totals.leads).toFixed(1):"—";const agencyRows=useMemo(()=>{const map=new Map();for(const r of rows){const key=r.siteSlug;if(!map.has(key))map.set(key,[]);map.get(key).push(r)}return [...map].map(([siteSlug,items])=>({siteSlug,...aggregate(items)})).sort((a,b)=>b.leads-a.leads)},[rows]);const channelRows=useMemo(()=>{const map=new Map();for(const r of rows){const key=r.utmSource||"direct";if(!map.has(key))map.set(key,[]);map.get(key).push(r)}return [...map].map(([source,items])=>({source,...aggregate(items)})).sort((a,b)=>b.leads-a.leads)},[rows]);return <MainLayout title="Cockpit acquisition" subtitle="Pilotage réseau des funnels d’acquisition Mondescale."><div className="space-y-6"><div className="bg-white rounded-2xl shadow-sm p-4 flex flex-wrap gap-4 items-end"><label className="text-sm font-medium text-slate-600">Période<select value={days} onChange={e=>setDays(Number(e.target.value))} className="block mt-1 border rounded-xl px-3 py-2 bg-white"><option value={7}>7 jours</option><option value={30}>30 jours</option><option value={90}>90 jours</option><option value={365}>12 mois</option></select></label><label className="text-sm font-medium text-slate-600">Agence<select value={site} onChange={e=>setSite(e.target.value)} className="block mt-1 border rounded-xl px-3 py-2 bg-white"><option value="">Tout le réseau</option>{Object.entries(AGENCIES).map(([slug,name])=><option key={slug} value={slug}>{name}</option>)}</select></label><div className="ml-auto text-xs text-slate-400">Attribution UTM · source / medium / campagne / contenu</div></div>{error&&<div className="bg-red-50 text-red-700 rounded-xl p-4">Erreur analytics : {error}</div>}{loading?<div className="bg-white rounded-2xl p-8 text-slate-500">Chargement des données…</div>:<><div className="grid grid-cols-2 lg:grid-cols-4 gap-4"><Card label="Leads" value={totals.leads}/><Card label="Leads HOT" value={totals.hot} sub={`${pct(totals.hot,totals.leads)} des leads`}/><Card label="Score moyen" value={avg}/><Card label="Notifications envoyées" value={totals.notificationSent} sub={pct(totals.notificationSent,totals.leads)}/><Card label="Contactés" value={totals.contacted} sub={pct(totals.contacted,totals.leads)}/><Card label="Convertis" value={totals.converted} sub={pct(totals.converted,totals.leads)}/><Card label="Taux contact → conversion" value={pct(totals.converted,totals.contacted)}/><Card label="Qualification" value={`${totals.hot}/${totals.warm}/${totals.cold}`} sub="HOT / WARM / COLD"/></div><div className="grid lg:grid-cols-2 gap-6"><section className="bg-white rounded-2xl shadow-sm p-5 overflow-x-auto"><h2 className="font-bold text-lg text-[#073653] mb-4">Performance par agence</h2><table className="w-full text-sm"><thead><tr className="text-left text-slate-500 border-b"><th className="py-2">Agence</th><th>Leads</th><th>HOT</th><th>Contactés</th><th>Convertis</th></tr></thead><tbody>{agencyRows.map(r=><tr key={r.siteSlug} className="border-b last:border-0"><td className="py-3 font-medium">{AGENCIES[r.siteSlug]||r.siteSlug}</td><td>{r.leads}</td><td>{r.hot}</td><td>{r.contacted}</td><td>{r.converted}</td></tr>)}</tbody></table>{!agencyRows.length&&<p className="text-slate-400 py-5">Aucun lead sur cette période.</p>}</section><section className="bg-white rounded-2xl shadow-sm p-5 overflow-x-auto"><h2 className="font-bold text-lg text-[#073653] mb-4">Performance par canal</h2><table className="w-full text-sm"><thead><tr className="text-left text-slate-500 border-b"><th className="py-2">Canal</th><th>Leads</th><th>HOT</th><th>Contactés</th><th>Convertis</th></tr></thead><tbody>{channelRows.map(r=><tr key={r.source} className="border-b last:border-0"><td className="py-3 font-medium">{CHANNELS[r.source]||r.source}</td><td>{r.leads}</td><td>{r.hot}</td><td>{r.contacted}</td><td>{r.converted}</td></tr>)}</tbody></table>{!channelRows.length&&<p className="text-slate-400 py-5">Aucune attribution sur cette période.</p>}</section></div><section className="bg-white rounded-2xl shadow-sm p-5 overflow-x-auto"><h2 className="font-bold text-lg text-[#073653] mb-4">Détail attribution</h2><table className="w-full text-xs"><thead><tr className="text-left text-slate-500 border-b"><th className="py-2">Agence</th><th>Source</th><th>Medium</th><th>Campagne</th><th>Contenu</th><th>Leads</th><th>Score</th></tr></thead><tbody>{rows.map((r,i)=><tr key={`${r.siteSlug}-${r.utmSource}-${r.utmContent}-${i}`} className="border-b last:border-0"><td className="py-3">{AGENCIES[r.siteSlug]||r.siteSlug}</td><td>{r.utmSource}</td><td>{r.utmMedium}</td><td>{r.utmCampaign}</td><td>{r.utmContent}</td><td>{r.leads}</td><td>{r.avgScore??"—"}</td></tr>)}</tbody></table></section><p className="text-xs text-slate-400">Les taux affichés portent sur les leads créés. Le taux visite → lead sera ajouté avec la télémétrie frontend, sans extrapoler les visites absentes des données actuelles.</p></>}</div></MainLayout>}
