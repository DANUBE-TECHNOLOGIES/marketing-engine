"use strict";
const express=require("express");
const {getCampaign}=require("./campaign-store");
const {listCampaignExecutions}=require("./campaign-execution-ledger");
const {loadCockpitState}=require("./network-cockpit-routes");
const {buildCampaignReport}=require("./campaign-report");
const {getFrozenCampaignReport,freezeCampaignReport}=require("./campaign-report-store");
const {evaluatePilotOutcome}=require("./pilot-outcome");
const {evaluateRecoveryTrustChain}=require("./recovery-trust-chain");
function criticalAlerts(state){return(state.propagationAlerts||[]).filter(item=>item.severity==="critical").length;}
function embeddedCriticalAlerts(report){return Number(report?.pilotEvidence?.criticalPropagationAlerts??0);}
async function predecessorReportForCampaign(prisma,campaign){const sourceCampaignId=campaign?.approvedScope?.sourceEvidenceCampaignId||null;if(!sourceCampaignId)return null;const frozen=await getFrozenCampaignReport(prisma,sourceCampaignId);return frozen?.report||null;}
async function recoveryTrustForCampaign(prisma,campaign){if(!campaign?.approvedScope?.recoveryOfCampaignId)return Object.freeze({ready:true,decision:"go",depth:0,rootCampaignId:campaign?.campaignId||null,blockers:Object.freeze([]),chain:Object.freeze([])});return evaluateRecoveryTrustChain(prisma,campaign);}
async function assertRecoveryReportEvidenceCurrent(prisma,campaign){const trust=await recoveryTrustForCampaign(prisma,campaign);if(trust.ready)return trust;const error=new Error(`Recovery trust chain NO-GO: ${(trust.blockers||[]).join(",")}`);error.status=409;error.code="RECOVERY_REPORT_EVIDENCE_NO_GO";error.blockers=trust.blockers||[];error.recoveryTrustChain=trust;throw error;}
function campaignReportRoutes({prisma}){const router=express.Router();
router.get("/api/presence/campaigns/:campaignId/report",async(req,res)=>{try{const campaign=await getCampaign(prisma,req.params.campaignId);if(!campaign)return res.status(404).json({ok:false,error:"Campagne Presence introuvable"});const[state,executions,frozen,predecessorReport,recoveryTrustChain]=await Promise.all([loadCockpitState(prisma),listCampaignExecutions(prisma,campaign.campaignId),getFrozenCampaignReport(prisma,campaign.campaignId),predecessorReportForCampaign(prisma,campaign),recoveryTrustForCampaign(prisma,campaign)]);const currentCriticalAlerts=criticalAlerts(state);const live=buildCampaignReport(campaign,state,executions,{criticalPropagationAlerts:currentCriticalAlerts,predecessorReport,recoveryTrustChain});const official=frozen?.report||live;const rollout=evaluatePilotOutcome(official,{criticalPropagationAlerts:frozen?embeddedCriticalAlerts(official):currentCriticalAlerts});return res.json({ok:true,persisted:Boolean(frozen),frozen:frozen?{createdAt:frozen.createdAt,report:frozen.report}:null,live,rollout,recoveryTrustChain});}catch(error){return res.status(error.status||500).json({ok:false,error:error.message,code:error.code,blockers:error.blockers,recoveryTrustChain:error.recoveryTrustChain});}});
router.post("/api/presence/campaigns/:campaignId/report/freeze",async(req,res)=>{try{if(req.body?.confirm!==true)return res.status(409).json({ok:false,error:"confirm=true requis pour figer le rapport final Presence"});const campaign=await getCampaign(prisma,req.params.campaignId);if(!campaign)return res.status(404).json({ok:false,error:"Campagne Presence introuvable"});if(!["completed","failed"].includes(campaign.status))return res.status(409).json({ok:false,error:"Le rapport final ne peut être figé que pour une campagne completed ou failed",status:campaign.status});const existing=await getFrozenCampaignReport(prisma,campaign.campaignId);if(existing){const recoveryTrustChain=await recoveryTrustForCampaign(prisma,campaign);return res.json({ok:true,persisted:true,immutable:true,alreadyFrozen:true,createdAt:existing.createdAt,report:existing.report,rollout:evaluatePilotOutcome(existing.report,{criticalPropagationAlerts:embeddedCriticalAlerts(existing.report)}),recoveryTrustChain});}const recoveryTrustChain=await assertRecoveryReportEvidenceCurrent(prisma,campaign);const[state,executions,predecessorReport]=await Promise.all([loadCockpitState(prisma),listCampaignExecutions(prisma,campaign.campaignId),predecessorReportForCampaign(prisma,campaign)]);const criticalPropagationAlerts=criticalAlerts(state);const report=buildCampaignReport(campaign,state,executions,{criticalPropagationAlerts,predecessorReport,recoveryTrustChain});const frozen=await freezeCampaignReport(prisma,campaign.campaignId,report);const rollout=evaluatePilotOutcome(frozen.report,{criticalPropagationAlerts:embeddedCriticalAlerts(frozen.report)});return res.status(201).json({ok:true,persisted:true,immutable:true,alreadyFrozen:false,createdAt:frozen.createdAt,report:frozen.report,rollout,recoveryTrustChain});}catch(error){return res.status(error.status||500).json({ok:false,error:error.message,code:error.code,blockers:error.blockers,recoveryTrustChain:error.recoveryTrustChain});}});return router;}
module.exports={campaignReportRoutes,criticalAlerts,embeddedCriticalAlerts,predecessorReportForCampaign,recoveryTrustForCampaign,assertRecoveryReportEvidenceCurrent};
