"use client";
import { useEffect, useMemo, useState } from "react";
import { indexationApi } from "../../lib/indexation-api";
const LABELS=Object.freeze({ready:"Prête",blocked:"Bloquée",awaiting_approval:"À approuver",approved:"Approuvée",succeeded:"Soumise",failed:"Échec",running:"En cours"});const READINESS_LABELS=Object.freeze({"read-only-ready":"connectée en lecture seule","oauth-token-missing":"connexion OAuth requise","feature-disabled":"fonction désactivée","property-not-accessible":"propriété inaccessible","search-console-api-error":"erreur API Google","readiness-error":"contrôle indisponible"});const asArray=v=>Array.isArray(v)?v:[];function runSiteSlug(r){return r?.sourcePlan?.siteSlug||r?.actions?.[0]?.payload?.siteSlug||null}function latestRunBySite(rs){const m=new Map;for(const r of asArray(rs)){const s=runSiteSlug(r);if(s&&!m.has(s))m.set(s,r)}return m}function operationalStatus(s,r){for(const x of["failed","succeeded","running","approved","awaiting_approval"])if(r?.status===x)return x;return s?.readyToSubmit===true?"ready":"blocked"}function readableIssue(x){return typeof x==="string"?x:x?.message||x?.code||JSON.stringify(x)}function StatusBadge({status}){return <span className={`indexation-status indexation-status-${status}`}>{LABELS[status]||status}</span>}function Metric({value,label}){return <div className="indexation-metric"><strong>{value}</strong><span>{label}</span></div>}function providerLabel(h,r){return r?.state?(READINESS_LABELS[r.state]||r.state):(h?.providerConfigured?"provider configuré":"provider non actif")}
export default function IndexationCockpitClient(){const[health,setHealth]=useState(null),[readiness,setReadiness]=useState(null),[candidates,setCandidates]=useState({sites:[]}),[history,setHistory]=useState({runs:[]}),[properties,setProperties]=useState({properties:[]}),[siteUrl,setSiteUrl]=useState(""),[operator,setOperator]=useState(""),[preflights,setPreflights]=useState({}),[busy,setBusy]=useState({}),[notice,setNotice]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(true);const applyResults=([h,r,c,hi,p])=>{if(h.status==="fulfilled")setHealth(h.value);if(r.status==="fulfilled"){setReadiness(r.value||null);if(r.value?.property)setSiteUrl(x=>x||r.value.property)}if(c.status==="fulfilled")setCandidates(c.value||{sites:[]});if(hi.status==="fulfilled")setHistory(hi.value||{runs:[]});if(p.status==="fulfilled"){setProperties(p.value||{properties:[]});const o=asArray(p.value?.properties).find(x=>x.permissionLevel==="siteOwner");if(o?.siteUrl)setSiteUrl(x=>x||o.siteUrl)}else setProperties({properties:[]});if(c.status==="rejected")setError(c.reason?.message||"Impossible de charger les candidats à l’indexation.")};const fetchCockpit=()=>Promise.allSettled([indexationApi.health(),indexationApi.readiness(),indexationApi.candidates(),indexationApi.history({limit:100}),indexationApi.properties()]);const load=async()=>{setLoading(true);setError("");applyResults(await fetchCockpit());setLoading(false)};useEffect(()=>{let cancelled=false;fetchCockpit().then(r=>{if(!cancelled){applyResults(r);setLoading(false)}});return()=>{cancelled=true}},[]);const latestRuns=useMemo(()=>latestRunBySite(history?.runs),[history]),rows=useMemo(()=>asArray(candidates?.sites).map(site=>{const run=latestRuns.get(site.siteSlug)||null;return{site,run,status:operationalStatus(site,run)}}),[candidates,latestRuns]),metrics=useMemo(()=>{const count=s=>rows.filter(r=>r.status===s).length;return{total:rows.length,ready:count("ready"),blocked:count("blocked"),approval:count("awaiting_approval")+count("approved"),submitted:count("succeeded"),failed:count("failed")}},[rows]);const runAction=async(key,action,msg)=>{setBusy(x=>({...x,[key]:true}));setError("");setNotice("");try{const result=await action();setNotice(msg);return result}catch(e){setError(e.message);return null}finally{setBusy(x=>({...x,[key]:false}))}};const preflight=async site=>{if(readiness?.ready!==true)return setError("Search Console doit être connectée et la propriété vérifiée avant le préflight.");if(!siteUrl)return setError("Sélectionnez d’abord la propriété Search Console propriétaire.");const r=await runAction(`${site.siteSlug}:preflight`,()=>indexationApi.preflight({siteSlug:site.siteSlug,siteUrl}),`Préflight validé pour ${site.siteSlug}. Aucune donnée n’a été envoyée à Google.`);if(r?.ready)setPreflights(x=>({...x,[site.siteSlug]:r}))};const prepare=async site=>{if(!preflights[site.siteSlug]?.ready)return setError("Un préflight valide est requis avant de préparer la soumission.");const r=await runAction(`${site.siteSlug}:prepare`,()=>indexationApi.prepare({siteSlug:site.siteSlug,siteUrl,sitemapUrl:site.sitemapUrl,requestedBy:operator.trim()||null}),`Soumission préparée pour ${site.siteSlug}. Elle attend maintenant une approbation explicite.`);if(r)await load()};const approve=async(site,run)=>{if(!operator.trim())return setError("Renseignez le nom de l’opérateur avant d’approuver.");const r=await runAction(`${site.siteSlug}:approve`,()=>indexationApi.approve({runId:run.id,approvedBy:operator.trim()}),`Soumission approuvée pour ${site.siteSlug}. Aucun envoi Google n’a encore eu lieu.`);if(r)await load()};const submit=async(site,run)=>{if(health?.submissionCapable!==true)return setError("Le provider Search Console actuel est en lecture seule. Aucune soumission Google n’est autorisée.");const r=await runAction(`${site.siteSlug}:submit`,()=>indexationApi.submit({runId:run.id}),`Sitemap de ${site.siteSlug} soumis à Search Console.`);if(r)await load()};const ownerProperties=asArray(properties?.properties).filter(p=>p.permissionLevel==="siteOwner"),providerReady=readiness?.ready===true,readOnly=readiness?.accessMode==="read-only"||health?.accessMode==="read-only",submissionCapable=health?.submissionCapable===true&&readiness?.submissionCapable===true,configuredProperty=readiness?.property||health?.configuredProperty||"sc-domain:agences.mondescale.com";
return <main className="indexation-shell"><header className="indexation-hero"><div><p className="indexation-eyebrow">Marketing Engine · SEO</p><h1>Opérations d’indexation</h1><p>Pilotez la readiness, le préflight et l’approbation Search Console agence par agence, avec des gardes explicites avant toute écriture Google.</p></div><button type="button" className="indexation-secondary" onClick={load} disabled={loading}>{loading?"Actualisation…":"Actualiser"}</button></header><section className={`provider-banner ${providerReady?"provider-ready":"provider-disabled"}`}><div><strong>Search Console : {providerLabel(health,readiness)}</strong><span>{health?.provider||readiness?.provider||"inconnu"}{health?.credentialMode?` · ${health.credentialMode}`:""}{readOnly?" · read-only":""}</span><span>Propriété : {configuredProperty}{readiness?.propertyAccess?.permissionLevel?` · ${readiness.propertyAccess.permissionLevel}`:""}</span></div><small>Approbation explicite obligatoire · Soumission automatique désactivée{!submissionCapable?" · Écriture Google désactivée":""}</small></section>{readiness?.state==="oauth-token-missing"?<div className="indexation-message indexation-notice"><strong>Connexion Search Console requise.</strong>{" "}Le consentement demandé est limité à la lecture Search Console. <a href="/api/search-console/auth">Connecter Search Console</a></div>:null}{readiness?.state==="feature-disabled"?<div className="indexation-message indexation-error">La connexion est prête côté application mais SEARCH_CONSOLE_ENABLED est encore désactivé dans le runtime.</div>:null}{readiness?.state==="property-not-accessible"?<div className="indexation-message indexation-error">Le compte Google connecté n’a pas accès à {configuredProperty}. Vérifiez les droits de propriété dans Search Console.</div>:null}{providerReady&&readOnly?<div className="indexation-message indexation-notice">Search Console est opérationnelle en lecture seule. Les contrôles de propriété, performances, sitemap et préflight sont actifs ; aucune soumission Google ne peut partir avec ce provider.</div>:null}<section className="indexation-controls"><label><span>Propriété Search Console</span>{ownerProperties.length?<select value={siteUrl} onChange={e=>setSiteUrl(e.target.value)}><option value="">Choisir une propriété propriétaire</option>{ownerProperties.map(p=><option key={p.siteUrl} value={p.siteUrl}>{p.siteUrl}</option>)}</select>:<input value={siteUrl||configuredProperty} onChange={e=>setSiteUrl(e.target.value)} placeholder="sc-domain:agences.mondescale.com"/>}</label><label><span>Opérateur d’approbation</span><input value={operator} onChange={e=>setOperator(e.target.value)} placeholder="Nom de l’opérateur"/></label></section><section className="indexation-metrics"><Metric value={metrics.total} label="agences candidates"/><Metric value={metrics.ready} label="prêtes"/><Metric value={metrics.blocked} label="bloquées"/><Metric value={metrics.approval} label="à valider"/><Metric value={metrics.submitted} label="soumises"/><Metric value={metrics.failed} label="échecs"/></section>{error?<div className="indexation-message indexation-error">{error}</div>:null}{notice?<div className="indexation-message indexation-notice">{notice}</div>:null}<section className="indexation-list">{loading&&!rows.length?<p>Chargement des mini-sites…</p>:null}{!loading&&!rows.length?<div className="indexation-empty">Aucun mini-site publié candidat à l’indexation.</div>:null}{rows.map(({site,run,status})=>{const blockers=asArray(site.blockers||site.readiness?.blockers),warnings=asArray(site.warnings||site.readiness?.warnings),preflightOk=preflights[site.siteSlug]?.ready===true,canStartFreshCycle=site.readyToSubmit===true&&(!run||run.status==="failed");return <article className="indexation-card" key={site.siteSlug}><div className="indexation-card-head"><div><h2>{site.siteName||site.agencyName||site.siteSlug}</h2><p>{site.siteSlug}</p></div><StatusBadge status={status}/></div><a className="sitemap-link" href={site.sitemapUrl} target="_blank" rel="noreferrer">{site.sitemapUrl}</a>{blockers.length?<div className="issue-box issue-blocker"><strong>Blocages</strong><ul>{blockers.map((x,i)=><li key={i}>{readableIssue(x)}</li>)}</ul></div>:null}{warnings.length?<div className="issue-box issue-warning"><strong>À améliorer</strong><ul>{warnings.map((x,i)=><li key={i}>{readableIssue(x)}</li>)}</ul></div>:null}{run?<div className="run-summary"><span>Dernière opération</span><strong>{run.status}</strong><small>{run.id}</small></div>:null}{run?.status==="failed"?<div className="indexation-message indexation-error">Le run précédent a échoué. Un nouveau préflight est obligatoire avant de créer une nouvelle soumission.</div>:null}{preflightOk?<div className="preflight-ok">Préflight validé · sitemap public et propriété propriétaire vérifiés.</div>:null}<div className="indexation-actions">{canStartFreshCycle?<><button type="button" className="indexation-secondary" onClick={()=>preflight(site)} disabled={busy[`${site.siteSlug}:preflight`]}>{busy[`${site.siteSlug}:preflight`]?"Vérification…":run?.status==="failed"?"Relancer le préflight":"Lancer le préflight"}</button><button type="button" onClick={()=>prepare(site)} disabled={!preflightOk||busy[`${site.siteSlug}:prepare`]}>Préparer une nouvelle soumission</button></>:null}{run?.status==="awaiting_approval"?<button type="button" onClick={()=>approve(site,run)} disabled={busy[`${site.siteSlug}:approve`]}>Approuver explicitement</button>:null}{run?.status === "approved" && submissionCapable?<button type="button" className="submit-button" onClick={()=>submit(site,run)} disabled={busy[`${site.siteSlug}:submit`]}>Soumettre à Google Search Console</button>:null}</div></article>})}</section></main>}
