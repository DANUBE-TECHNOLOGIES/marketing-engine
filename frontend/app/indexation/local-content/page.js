import LocalContentUniquenessClient from "./LocalContentUniquenessClient";

export const metadata = {
  title: "Unicité des contenus locaux | Marketing Engine",
  robots: { index: false, follow: false },
};

export default function LocalContentUniquenessPage() {
  return <LocalContentUniquenessClient />;
}
